#include <nba/core/bitmap.hh>
#include <vector>
#include <gtest/gtest.h>
#if 0 // for build scripts
#require <core/bitmap.o>
#endif

using namespace nba;

TEST(CoreBitmapTest, Dummy) {
    EXPECT_TRUE(1);
}

// vim: ts=8 sts=4 sw=4 et
